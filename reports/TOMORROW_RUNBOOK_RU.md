# Runbook на завтра (после докачки Sentinel-2)

Дата подготовки: 2026-06-08, 21:0x.

## Статус на сейчас

- Фоновая выгрузка S2 **жива** (pid 37045), идёт по порядку: сначала весь `07_01`, потом `08_01`, потом `09_01`.
- Покрытие S2 по строкам v26: `07_01` ≈ 88%, `08_01` ≈ 17%, `09_01` = 0%.
- Скорость ~450 строк/час, осталось ~2200 → к утру `08_01` (наша главная дата) должен закрыться, `09_01` — частично.
- Конструкция модели проверена: walk-forward корректен (предсказываются 2023–2024–2025; 2021–2022 только train), метрики только по предсказанным строкам, бенчмарк Cropwise на тех же строках, pre-harvest фильтр включён.

## Что сделать завтра (3 команды)

```bash
# 1. Проверить, что S2 докачался (08_01 близко к 100%)
python3 - <<'PY'
import pandas as pd
s=pd.read_csv('data_raw/sentinel2_indices_b1.csv')
print(s[s.status=='ok'].asof_tag.value_counts().to_dict())
PY

# 2. Если 08_01 ещё не полный — добрать (resume, безопасно):
python3 scripts/external/fetch_sentinel2_indices_b1.py --cloud-lt 35

# 3. Пересобрать v27 и оценить вклад S2:
python3 scripts/asof/build_v27_s2_harvest_dataset.py
python3 scripts/asof/evaluate_v27_s2.py
```

Результат: `reports/V27_S2_FINAL_RU.md` + `models_v2/asof_comparison/asof_results_v27_s2.csv`.

## Нужен ли ещё прогон по данным?

Данные — **нет**, новый аудит не требуется: target/единицы/даты/crop-join/дубли/диапазоны проверены,
pre-harvest баг исправлен (`scripts/audit/audit_v26_microscope.py` воспроизводит всё).

Единственная незакрытая мелочь: ~172 поля-года v26 не было в старом S2-worklist (он строился от v24).
Это уже исправлено — `load_tasks()` теперь берёт v26, поэтому команда resume (шаг 2) сама их добёрет.

## Ранний сигнал S2 (на неполных данных, для ориентира)

- wheat `07_01`: R² 0.365 → **0.415**, MAPE 32.0 → **30.4** с добавлением S2 — то, что и обещает литература (red-edge помогает пшенице).
- barley `07_01`: MAPE 44.2 → **41.6**.

Полный честный вывод — после докачки `08_01`.

## Следующее после v27
- v28: менеджмент-фичи из `data_clean/operations_clean.csv` + NPK через словари `fertilizers`/`chemicals`.
- Озимые: добавить осенний NDVI предыдущего года (ограничение из микроскоп-аудита).
